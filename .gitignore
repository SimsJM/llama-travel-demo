# Secrets — never commit these
.env
.env.*
!.env.example
*.pem

# Dependencies, if a backend is added later
node_modules/

# OS cruft
.DS_Store
Thumbs.db
desktop.ini

# Server dependencies
server/node_modules/
server/.env
